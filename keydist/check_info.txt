

// debugging point. 
//  - gcl ref cnt /  freehandle 
//  - when gcl is dropped in cache, the info can be refelcted at ksData 

// later kds_client_api 

// current: ex_k name : KEY_{LOG NAME} 
// later : independent ex_k name / based on metadata... 
kds_create()
{
	// CMD_create  with metadata... 
	if( GDP_ACK_SUCCESS )  : don't need append  : incref 
	else if( GDP_ACK_CREATED ) :; need append 

	// CMD_OPEN  : check whether this can be skipped... 
	// CMD_append 

	CMD_CLOSE() .


// In each command, gcl_refcnt is up and down . 
// ex open/close ... : starting time: ++ / end time: -- with *_decref. 

}

